{
	"name": "complexitytheory-ci",
	"private": true,
	"type": "module",
	"scripts": {
		"check": "tsc --noEmit && hollywood check --generated --source-root ci --output . && hollywood check --workflow-security --source-root .github/workflows --output .",
		"generate": "hollywood generate \"ci/*.ts\" --output ."
	},
	"devDependencies": {
		"@dedalus-labs/hollywood": "0.0.1",
		"@types/node": "26.0.1",
		"typescript": "6.0.3"
	},
	"engines": {
		"node": "26.5.1"
	},
	"packageManager": "npm@11.17.0"
}
