# Agent Configuration

This directory is the canonical home for repository-scoped agent tooling.
Compatibility directories point here; repository instructions begin in
`AGENTS.md`, which imports `STYLE.md`.
